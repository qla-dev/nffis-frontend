# Local Vite proxy target.

# Optional complete API URL override (takes precedence over the selector).
# VITE_NFFIS_API_URL=https://api.nffis.com/api
#VITE_API_BASE_URL=https://api.nffis.com:81/api for local development with backend running on port 81 (docker container)
VITE_LOCAL_API_ORIGIN=http://localhost:8081 # for local development with backend running on port 8081
