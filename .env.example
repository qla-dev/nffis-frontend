
# No API environment configuration is needed.
# Local development uses Vite's same-origin proxy to http://localhost:8080.
# VITE_LOCAL_API_ORIGIN=http://localhost:8080
# To use a remote API through that same proxy, set its full API URL:
# VITE_API_BASE_URL=https://api.nffis.com:81/api
